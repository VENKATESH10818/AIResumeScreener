# ============================================================
# AI Resume Screener — Python dependencies (conflict-free)
# ============================================================

# Web framework — use plain fastapi (no [standard] extra) to avoid
# fastapi-cli pulling in typer>=0.12 which conflicts with spaCy 3.7.
# uvicorn is listed separately instead.
fastapi==0.111.0
uvicorn==0.29.0
python-multipart==0.0.9
httptools==0.6.1
websockets==12.0

# Database — async SQLite
sqlalchemy==2.0.30
aiosqlite==0.20.0
# Uncomment for PostgreSQL:
# asyncpg==0.29.0

# Resume parsing
pdfplumber==0.11.0
python-docx==1.1.2
pytesseract==0.3.10
Pillow==10.3.0

# NLP & NER — spaCy 3.8.x supports typer>=0.12, resolving the conflict
spacy==3.8.2

# Embeddings
sentence-transformers==3.0.1

# Vector search
faiss-cpu==1.8.0

# Utilities
python-dotenv==1.0.1
regex==2024.4.16
pydantic==2.7.1
pydantic-settings==2.2.1
numpy==1.26.4
